# @interop/edv-conformance-suite Changelog

## 0.1.0 - TBD

### Added

- Initial implementation: conformance suites 01-07 (vault config, documents,
  sequence invariants, encrypted-index queries, chunks/streams, zcap
  authorization, wire format), hybrid edv-client + raw-HTTP drivers, local
  (no-WebKMS) key material, pluggable vault provisioning, `edv-conformance` CLI,
  and example/bedrock configurations.
